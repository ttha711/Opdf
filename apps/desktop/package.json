{
  "name": "@opdf/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "npm run build:main && npm run build:preload && cross-env OPDF_DEV_SERVER=http://localhost:5174 OPDF_PROJECT_PATH=C:/mymy/Opdf electron .",
    "build": "npm run build:main && npm run build:preload",
    "build:main": "tsc -p tsconfig.main.json",
    "build:preload": "tsc -p tsconfig.preload.json",
    "typecheck": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.preload.json --noEmit",
    "lint": "npm run typecheck",
    "package": "electron-builder"
  },
  "dependencies": {
    "@opdf/core": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "cross-env": "^7.0.3",
    "electron": "^32.2.2",
    "electron-builder": "^24.13.3",
    "typescript": "^5.8.3"
  },
  "build": {
    "appId": "com.opdf.desktop",
    "files": [
      "dist/**/*",
      "../web/dist/**/*"
    ],
    "directories": {
      "output": "release"
    },
    "win": {
      "target": "nsis"
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
